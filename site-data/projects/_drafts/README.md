# Project copy drafts

Copy `outline-template.md` to `<project>.md` here to start a draft. Move approved
copy to the parent directory to use it on that project's full page. Homepage
cards and the Software directory always use the concise checked description in
`site-data/projects.toml`.
